const { Mintable } = require('../../../../../../index.js')
const { MyMintable } = require('../../../../../common/my-mintable.js')

console.log('In @scoped/reviewed')
try {
  require.keys.unboxStrict(Mintable.minterFor(MyMintable), () => true)
  console.log('pass')
} catch (exc) {
  console.log(exc.message)
}
