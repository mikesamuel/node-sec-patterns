const { Mintable } = require('../../../../../index.js')

// Create two types so we can test loading of @bar/Bar/minimal.json
// configuration versus configuration in package.json
class MTA extends Mintable {}
class MTB extends Mintable {}

Object.defineProperty(MTA, 'contractKey', { value: 'CK-A' })
Object.defineProperty(MTB, 'contractKey', { value: 'CK-B' })

module.exports = { MTA, MTB }
